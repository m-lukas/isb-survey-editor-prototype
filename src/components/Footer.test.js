import React from "react";
import { render } from "@testing-library/react";

import Footer from "./Footer";

const makeSut = (props) => {
    return render(
        <Footer />
    )
}

describe('<Footer />', () => {
    it('', () => {})
})