import React from "react";
import { render } from "@testing-library/react";

import CardInfo from "./CardInfo";
const makeSut = (props) => {
    return render(
        <CardInfo />
    )
}

describe('<CardInfo />', () => {
    it('', () => {})
})