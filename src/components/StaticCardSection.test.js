import React from "react";
import { render } from "@testing-library/react";

import StaticCardSection from "./StaticCardSection";

const makeSut = (props) => {
    return render(
        <StaticCardSection />
    )
}

describe('<StaticCardSection />', () => {
    it('', () => {})
})