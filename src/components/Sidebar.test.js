import React from "react";
import { render } from "@testing-library/react";

import Sidebar from "./Sidebar";

const makeSut = (props) => {
    return render(
        <Sidebar />
    )
}

describe('<Sidebar />', () => {
    it('', () => {})
})