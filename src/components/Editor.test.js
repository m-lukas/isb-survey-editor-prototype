import React from "react";
import { render } from "@testing-library/react";

import Editor from "./Editor";

const makeSut = (props) => {
    return render(
        <Editor />
    )
}

describe('<Editor />', () => {
    it('', () => {})
})