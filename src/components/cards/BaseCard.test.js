import React from "react";
import { render } from "@testing-library/react";

import BaseCard from "./BaseCard";

const makeSut = (props) => {
    return render(
        <BaseCard />
    )
}

describe('<BaseCard />', () => {
    it('', () => {})
})