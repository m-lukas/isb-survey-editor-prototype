import React from "react";
import { render } from "@testing-library/react";

import PopupMenu from "./PopupMenu";

const makeSut = (props) => {
    return render(
        <PopupMenu />
    )
}

describe('<PopupMenu />', () => {
    it('', () => {})
})